# fonts

This folder is for user-supplied font files.

- Do not commit proprietary or system fonts unless you are sure redistribution is allowed.
- The repository intentionally keeps this folder empty by default.
- Put your local `ttf` / `otf` / `ttc` files here when testing.
